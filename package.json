{
  "name": "paper-stories",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/cli/validate.test.js packages/cli/local.test.js && npm test --workspace=packages/viewer"
  }
}
