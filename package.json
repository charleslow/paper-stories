{
  "name": "paper-stories",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/cli/validate.test.js && npm test --workspace=packages/viewer"
  }
}
